<UserControl x:Class="AmberBases.UI.DictionaryTableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar with Undo/Redo -->
        <ToolBar Grid.Row="0" Height="30">
            <Button x:Name="UndoButton" Click="UndoButton_Click" ToolTip="Отменить (Ctrl+Z)" Padding="5,2">
                <TextBlock FontSize="14">↩</TextBlock>
            </Button>
            <Button x:Name="RedoButton" Click="RedoButton_Click" ToolTip="Повторить (Ctrl+Y)" Padding="5,2">
                <TextBlock FontSize="14">↪</TextBlock>
            </Button>
            <Separator/>
            <TextBlock Text="Undo/Redo" VerticalAlignment="Center" Margin="5,0" Foreground="Gray" FontSize="11"/>
        </ToolBar>

        <!-- Header with title -->
        <Border Grid.Row="1" Background="#e0e0e0" Padding="10,5">
            <TextBlock x:Name="TitleTextBlock" FontSize="14" FontWeight="Bold" Text="Таблица"/>
        </Border>

        <!-- Search bar -->
        <Border Grid.Row="2" Background="#f0f0f0" Padding="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="5,0"/>
                <TextBox x:Name="SearchTextBox" Width="200" Margin="5" TextChanged="SearchTextBox_TextChanged"/>
                <TextBlock Text="Столбец:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <ComboBox x:Name="SearchColumnCombo" Width="150" Margin="5" SelectionChanged="SearchColumnCombo_SelectionChanged"/>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid x:Name="MainDataGrid" Grid.Row="3"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  SelectionMode="Extended"
                  SelectionUnit="CellOrRowHeader"
                  SelectedCellsChanged="MainDataGrid_SelectedCellsChanged"
                  PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown"
                  PreviewKeyDown="DataGrid_PreviewKeyDown">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Добавить строку" Click="ContextMenu_AddRow_Click"/>
                    <Separator/>
                    <MenuItem Header="Редактировать" Click="ContextMenu_Edit_Click"/>
                    <MenuItem Header="Удалить строку" Click="ContextMenu_DeleteRow_Click"/>
                    <Separator/>
                    <MenuItem Header="Копировать" Click="ContextMenu_Copy_Click"/>
                    <MenuItem Header="Вставить из Excel" Click="ContextMenu_PasteExcel_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</UserControl>