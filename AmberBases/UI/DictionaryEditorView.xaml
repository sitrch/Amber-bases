<UserControl x:Class="AmberBases.UI.DictionaryEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AmberBases.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition x:Name="DictionarySelectorRow" Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top ToolBar / Search -->
        <Border Grid.Row="0" Background="#f0f0f0" Padding="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="SearchTextBox" Width="200" Margin="5" TextChanged="SearchTextBox_TextChanged"/>
            </StackPanel>
        </Border>

        <!-- Dictionary selector -->
        <Border x:Name="DictionarySelectorBorder" Grid.Row="1" Background="#e0e0e0" Padding="5">
            <StackPanel Orientation="Horizontal">
                <Button Content="System Providers" Click="BtnShowGrid_Click" Tag="SystemProvidersGrid" Margin="2" Padding="5,2"/>
                <Button Content="Profile Systems" Click="BtnShowGrid_Click" Tag="ProfileSystemsGrid" Margin="2" Padding="5,2"/>
                <Button Content="Colors" Click="BtnShowGrid_Click" Tag="ColorsGrid" Margin="2" Padding="5,2"/>
                <Button Content="Whip Lengths" Click="BtnShowGrid_Click" Tag="WhipLengthsGrid" Margin="2" Padding="5,2"/>
                <Button Content="Profile Types" Click="BtnShowGrid_Click" Tag="ProfileTypesGrid" Margin="2" Padding="5,2"/>
                <Button Content="Applicabilities" Click="BtnShowGrid_Click" Tag="ApplicabilitiesGrid" Margin="2" Padding="5,2"/>
                <Button Content="Profile Articles" Click="BtnShowGrid_Click" Tag="ProfileArticlesGrid" Margin="2" Padding="5,2"/>
                <Button Content="Coating Types" Click="BtnShowGrid_Click" Tag="CoatingTypesGrid" Margin="2" Padding="5,2"/>
            </StackPanel>
        </Border>

        <!-- GridSplitter для регулировки высоты панели справочников -->
        <GridSplitter x:Name="DictionarySelectorSplitter" Grid.Row="2" Height="5" HorizontalAlignment="Stretch" 
                      Background="Transparent" ResizeDirection="Rows" ResizeBehavior="PreviousAndNext"/>

        <!-- Grids -->
        <Grid Grid.Row="3" Margin="5">
            <local:DictionaryTableControl x:Name="SystemProvidersGrid" Visibility="Visible"/>
            <local:DictionaryTableControl x:Name="ProfileSystemsGrid" Visibility="Collapsed"/>
            <local:DictionaryTableControl x:Name="ColorsGrid" Visibility="Collapsed"/>
            <local:DictionaryTableControl x:Name="WhipLengthsGrid" Visibility="Collapsed"/>
            <local:DictionaryTableControl x:Name="ProfileTypesGrid" Visibility="Collapsed"/>
            <local:DictionaryTableControl x:Name="ApplicabilitiesGrid" Visibility="Collapsed"/>
            <local:DictionaryTableControl x:Name="ProfileArticlesGrid" Visibility="Collapsed"/>
            <local:DictionaryTableControl x:Name="CoatingTypesGrid" Visibility="Collapsed"/>
        </Grid>

        <!-- Bottom Actions -->
        <Border Grid.Row="4" Background="#f0f0f0" Padding="5">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="UndoBtn" Content="Undo" Click="UndoBtn_Click" Margin="5" Padding="10,5" IsEnabled="False"/>
                <Button x:Name="RedoBtn" Content="Redo" Click="RedoBtn_Click" Margin="5" Padding="10,5" IsEnabled="False"/>
                <Separator Margin="5"/>
                <Button Content="Refresh Data" Click="BtnRefresh_Click" Margin="5" Padding="10,5"/>
                <Button Content="Save Changes" Click="BtnSave_Click" Margin="5" Padding="10,5"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>